import pmel.sdig.las.Dataset

model {
    List<Dataset> datasets
}
json {
    datasets {
        datasets.each { datasetInstance ->
            dataset {
                id datasetInstance.id
                title datasetInstance.title
            }
        }
    }
}